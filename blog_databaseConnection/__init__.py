# __init__.py
# This empty file marks the 'blog_databaseConnection' directory as a Python package.
# Without this file, Python would not recognize the directory as a module,
# and relative imports like 'from . import model' in main.py would fail.
